<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/doa_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/doa_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doaapp"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.activity.fragments.fragment_user"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.fragments.fragment_add_publication"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.fragments.fragment_home"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.MainActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.activity.home"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>